# Chuyển tất cả các file văn bản sang LF khi lưu trên GitHub
* text=auto

# Cố định một số loại file cụ thể thành LF
*.sh eol=lf
*.js eol=lf
*.css eol=lf

# Cố định file thành CRLF (nếu cần)
*.bat eol=crlf

